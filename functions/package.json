{
  "name": "yogaflow-functions",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "rrule": "^2.8.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": "20"
  }
}
